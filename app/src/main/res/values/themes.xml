<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/gradientStart</item>
        <item name="colorPrimaryVariant">@color/warnatext</item>
        <item name="colorOnPrimary">@color/warnatext</item> <!-- Warna teks & ikon di Toolbar -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:colorBackground">@color/background_dark</item>

        <!-- Warna ikon di AppBar -->
        <item name="colorControlNormal">@color/white</item>
    </style>
    <style name="RoundedToolbarShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">40dp</item>
        <item name="cornerSizeTopRight">40dp</item>
        <item name="cornerSizeBottomLeft">16dp</item>
        <item name="cornerSizeBottomRight">16dp</item>
    </style>

        </resources>